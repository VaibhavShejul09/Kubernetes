apiVersion: v1
kind: Service
metadata:
  name: auth-backend-service
  namespace: my-app
spec:
  selector:
    app: auth-backend
  ports:
    - protocol: TCP
      port: 8081
      targetPort: 8081
  type: ClusterIP

