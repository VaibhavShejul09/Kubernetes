# Redis Headless Service (for StatefulSet)
apiVersion: v1
kind: Service
metadata:
  name: redis
  namespace: my-app
spec:
  clusterIP: None
  selector:
    app: redis
  ports:
    - port: 6379
      targetPort: 6379
