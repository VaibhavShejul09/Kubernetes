apiVersion: v1
kind: Service
metadata:
  name: api-gateway-service
  namespace: my-app
spec:
  selector:
    app: apigateway-backend
  ports:
    - protocol: TCP
      port: 8080
      targetPort: 8080
  type: ClusterIP

