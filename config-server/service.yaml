apiVersion: v1
kind: Service
metadata:
  name: config-server-service
  namespace: my-app
spec:
  selector:
    app: configserver-backend
  ports:
    - protocol: TCP
      port: 8888
      targetPort: 8888
  type: ClusterIP

