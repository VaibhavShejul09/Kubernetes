apiVersion: v1
kind: Service
metadata:
  name: problem-backend-service
  namespace: my-app
spec:
  selector:
    app: problem-backend
  ports:
    - protocol: TCP
      port: 8082
      targetPort: 8082
  type: ClusterIP

